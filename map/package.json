{
  "name": "pnw-repeater-zone-selector",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "validate:regions": "node ./scripts/validate-regions.mjs",
    "test:fixtures": "node ./scripts/test-fixtures.mjs",
    "check": "npm run validate:regions && npm run test:fixtures"
  }
}
